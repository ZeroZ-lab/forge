# CU-20260613-v0.36.2-release

## Why

Publish the token-footprint optimization and value-scenario benchmark coverage as a patch release.

## Behavior Change

- Bumped package/plugin version metadata from `0.36.1` to `0.36.2`.
- Synchronized:
  - `package.json`
  - `plugins/forge/.claude-plugin/plugin.json`
  - `plugins/forge/.codex-plugin/plugin.json`

## Impact Scope

- Release metadata only.
- No runtime skill behavior changed by this Change Unit beyond the already documented token optimization and benchmark coverage changes.

## Risk

- If any of the three version files drift, users may see different versions in npm/package metadata and plugin UIs. Validation includes an explicit version consistency check before commit.

## Verification Evidence

- Version consistency check confirms all three files are `0.36.2`.
- `node scripts/measure-token-footprint.mjs --max-default-chain-chars=9000 --max-total-chars=56000`
- `node scripts/evaluate-skills.mjs`
- `node scripts/validate.mjs`
- `node --test 'tests/*.test.mjs'`
- `git diff --check`
