# CU-20260702-v0.48.0-release

## Type

Release

## Intent

- Trigger：A1/A2/A3/A4 四个方法论小迭代已落地并通过回归，需要同步 package/plugin 版本并推送发布点。
- Goal：将 Forge 版本从 `0.47.0` bump 到 `0.48.0`，同步 npm package 与 Claude/Codex plugin manifest。
- Out of scope：创建 tag；开 PR；改 eval oracle、manifest skill 列表或发布渠道配置。

## Behavior Change

- 版本号 `0.47.0` → `0.48.0`。
- 本 release 汇总以下方法论变更：
  - detail module 粒度双轴判据。
  - research buy-vs-build 矩阵。
  - delegation decision matrix。
  - learn current-project / cross-project boundary。

## Affected Surface

- `package.json`
- `plugins/forge/.claude-plugin/plugin.json`
- `plugins/forge/.codex-plugin/plugin.json`
- `docs/change-units/CU-20260702-v0.48.0-release.md`

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| 版本文件不同步 | 插件 UI / package metadata 显示漂移 | `npm run validate` 检查三处 version 一致 |
| 本地 master 推送包含前序未推送提交 | 远端一次收到多个发布相关 commit | 用户显式要求 commit and push；推送前验证通过 |
| 未打 tag | 远端没有 `v0.48.0` tag 锚点 | 本次用户只要求 commit and push，tag 留给后续显式发布动作 |

## Verification

```bash
npm run bump -- 0.48.0
```

Result: package.json, Claude plugin manifest, and Codex plugin manifest changed `0.47.0 -> 0.48.0`.

```bash
npm test
```

Result: 87 pass / 0 fail.

```bash
npm run validate
```

Result: `Forge validation passed (25 skills, version 0.48.0).`

```bash
npm run eval:skills
```

Result: `Forge skills-suite benchmark contract passed (21 cases, 25 skills covered).` No run report supplied; behavioral effectiveness is not claimed.

```bash
npm run metrics:chars
```

Result: default chain 4281 chars; all SKILL.md 48668 chars.

## Unverified

- No real Codex benchmark run report was supplied.
- No tag was created for `v0.48.0` in this step.

## Rollback

Revert the release commit or run `node scripts/bump-version.mjs 0.47.0` and commit a rollback. If already pushed, use a new revert commit rather than rewriting remote history.

## Authoritative Documents Synchronized

- `package.json` version is `0.48.0`.
- `plugins/forge/.claude-plugin/plugin.json` version is `0.48.0`.
- `plugins/forge/.codex-plugin/plugin.json` version is `0.48.0`.
