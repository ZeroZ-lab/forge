# CU-20260702-v0.49.0-release

## Type

Release

## Intent

- Trigger：effectiveness feedback loop 已落地，需要同步 package/plugin 版本并推送发布点。
- Goal：将 Forge 版本从 `0.48.0` bump 到 `0.49.0`，同步 npm package 与 Claude/Codex plugin manifest。
- Out of scope：创建 tag；开 PR；运行真实 Codex effectiveness benchmark。

## Behavior Change

- 版本号 `0.48.0` → `0.49.0`。
- 本 release 包含：
  - 独立 `evals/effectiveness-suite/` contract。
  - `npm run eval:effectiveness` 与 validate 接入。
  - detail vertical slice 约束。
  - review buy-vs-build lens。
  - learn `Cross-project candidates` 输出格式。
  - guide delegation matrix 建议。

## Affected Surface

- `package.json`
- `plugins/forge/.claude-plugin/plugin.json`
- `plugins/forge/.codex-plugin/plugin.json`
- `docs/change-units/CU-20260702-v0.49.0-release.md`

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| 版本文件不同步 | 插件 UI / package metadata 显示漂移 | `npm run validate` 检查三处 version 一致 |
| effectiveness contract 被误当效果证明 | 对外过度声明 | 脚本、README、CU 都声明 no run report / no effectiveness claim |
| 未打 tag | 远端没有 `v0.49.0` tag 锚点 | 本次用户只要求 bump、commit、push，tag 留给显式发布动作 |

## Verification

```bash
npm run bump -- 0.49.0
```

Result: package.json, Claude plugin manifest, and Codex plugin manifest changed `0.48.0 -> 0.49.0`.

```bash
npm run eval:effectiveness
```

Result: `Forge effectiveness-suite contract passed (5 held-out cases, 5 scenarios, 2 repeats required).` No run report supplied; real-world effectiveness is not claimed.

```bash
npm test
```

Result: 90 pass / 0 fail.

```bash
npm run validate
```

Result: `Forge validation passed (25 skills, version 0.49.0).`

```bash
npm run eval:skills
```

Result: `Forge skills-suite benchmark contract passed (21 cases, 25 skills covered).` No run report supplied; behavioral effectiveness is not claimed.

```bash
npm run metrics:chars
```

Result: default chain 4433 chars; all SKILL.md 49072 chars.

## Unverified

- No real Forge/no-Forge effectiveness benchmark run was executed.
- No `v0.49.0` tag was created.

## Rollback

Revert the release commit or run `node scripts/bump-version.mjs 0.48.0` and commit a rollback. If already pushed, use a new revert commit rather than rewriting remote history.

## Authoritative Documents Synchronized

- `package.json` version is `0.49.0`.
- `plugins/forge/.claude-plugin/plugin.json` version is `0.49.0`.
- `plugins/forge/.codex-plugin/plugin.json` version is `0.49.0`.
