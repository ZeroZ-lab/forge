# CU-20260625-v0.40.0-release

## Type

- Release

## Intent

- Trigger: 对抗式审计后的完整性恢复 + 评测能力增量已落地（4 个 commit），版本从 0.39.0（此前为"幽灵发布"，首次真正入库于 c205a08）升 0.40.0。
- Goal: 给本轮工作一个显式发布点，打 tag，push 到 origin，让"绿线 + 诚实度量 + 磁盘可验评测"的状态首次上远端。
- Out of scope: 历史 0.37–0.39 拆分为独立 commit（不重写已发布历史）；行为有效性 benchmark（需 Codex CLI + 额度，单独进行）。

## 本版本包含（commit 索引）

- `c205a08` v0.39.0 integrity restoration — validator/test 转绿（default chain 9561→8947）、char 度量诚实化、README 真实化、D10 notes/ 漂移修复；同时把此前未提交的 document-artifact-policy WIP 落地。
- `10079ed` feat: eval `--verify-disk` — 报告声称的 Change Unit 必须落盘（自报→磁盘可验）。
- `56f3ced` chore: rename `measure-char-footprint` / `metrics:chars`（命名与诚实输出对齐）。
- `5342d6b` feat: M1 内容校验 + H4 角色去歧义 — `--verify-disk` 再校验 CU 含 `## Verification` + 命令证据；`test`/`design` description 改写为协调者。

详见对应 CU：`CU-20260625-integrity-restoration`、`-eval-verify-disk`、`-audit-followthrough`。

## Behavior Change（对外）

- 新增评测能力 `--verify-disk`（默认关闭）。
- `metrics:tokens` → `metrics:chars`；`measure-token-footprint.mjs` → `measure-char-footprint.mjs`。
- `detail/codegen/review` SKILL.md 文案精简（default chain 字符数下降）；`test`/`design` description 措辞变更（when_to_use 不变）。
- 版本号 0.39.0 → 0.40.0（package.json + 两个 plugin manifest 同步）。

## Affected Surface

- `package.json`、`plugins/forge/.claude-plugin/plugin.json`、`plugins/forge/.codex-plugin/plugin.json`
- 新增本 CU

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| 单 release commit 合并多版本 WIP | 历史难拆 | 已在前序 CU 显式记录；不重写历史 |
| `--verify-disk` 默认关闭，无人启用 | 能力空置 | README 记录；CI 真实运行时建议启用 |
| description 改动影响 skill 路由 | 触发变化 | when_to_use 未变；无行为测试覆盖路由语义（已知缺口） |
| push 后不可逆 | 远端可见 | 用户显式授权 push |

## Verification

- Commands: `node scripts/validate.mjs`、`node --test 'tests/*.test.mjs'`、`node scripts/measure-char-footprint.mjs`
- Results: validate exit 0（25 skills, version **0.40.0**）；tests 28 pass / 0 fail；default chain 8947<9000、total 50963<56000。
- 发布动作：`git tag v0.40.0` + `git push origin master --tags`。

## Rollback

版本号回退：`node scripts/bump-version.mjs 0.39.0` + revert release commit。已 push 的 commit/tag 只能新增回滚 commit，不可从远端抹除。

## 权威文档同步

- 三处版本号同步至 0.40.0。
- 本 release CU 汇总本轮 4 个功能 commit，详细证据见各自 CU。
