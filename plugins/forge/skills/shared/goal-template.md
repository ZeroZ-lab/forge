# <Feature Name>

> 一句话：这个 feature 做什么、为谁做。
> 读取提示：前 4 节（目标 / 边界 / 完成标准）= 首屏合约，读完即可开工；
> 实现细节在「需要细节时」的指针里，按需再读。

<!--
首屏合约纪律（agent 友好）：
- 目标 / 边界 / 完成标准 必须落在文件前 ~30 行内，让只读一个读窗的 agent 拿到全部硬约束。
- 不确定的需求或实现，写进「待澄清」，不要默默假设（D6）。
- 实现细节（表结构、端点字段、目录）放 modules/，本文件只留指针（D2/D4）。
-->

## 目标

> 这个功能要达成什么效果。以用户可感知的行为描述。

- ...

## 边界

### 包含
- ...

### 不包含
- ...

### 约束（硬约束）
- 安全、性能、兼容性等约束
- 引用 project.md 的共享约束（PD#），只补充本 feature 新增

> **阈值单源**：跨多个模块共用的阈值/边界配置（如 warning/danger 阈值），集中在本节或一个专门的 thresholds 模块定义一次，各模块引用，不在各自模块内联重写。
> **关键布局单源**：被多个模块引用的关键布局结构（如整体 Grid 的 grid-template-areas、面板定位关系），在 goal.md 或一个权威 module 给出结构定义，不散落在多处注释里。
> **边界（保 D2/D4）**：这里只定义"被可度量验收条件引用、或被多模块共用"的约束。不要把每个数据字段都强行加范围、也不要写像素级 CSS——没有验收条件依赖的实现细节由 AI 在 codegen 时自主决定。模拟/演示类数据字段可标注合理游走区间供模拟器使用。

## 完成标准

> 怎么判断做完了。每条优先用 EARS 句式，让验收条件天然可测、可被 codegen 自检、被 review 逐条打勾。
> 句式：`WHEN <事件/前置条件>，系统 SHALL <可观察、可验收的行为>`。
> 状态型条件可用 `WHILE <状态>`；可选触发可用 `WHERE <特性存在>`。纯展示类无明确触发时可退化为陈述句，但仍须可验收。
> Goal coverage：若本 feature 会修改代码、测试、模块或发布面，在本节或「按需约束」列出覆盖路径（如 `src/...`、`tests/...`、`docs/features/<feature>/modules/...`），让 codegen/review 能证明产物受此 goal 约束。

- [ ] WHEN <事件/前置>，系统 SHALL <可验收行为>
- [ ] WHEN <事件/前置>，系统 SHALL <可验收行为>
- [ ] 关键场景覆盖：<场景> SHALL <预期结果>

## 决策记录

| # | 决策 | 选择 | 理由 | 拒绝 |
|---|------|------|------|------|
| 1 | | | | |

## 按需约束

> 仅在没有独立文档必要时使用。内容较复杂且有独立 owner / 审批 / 更新周期时，按 artifact policy 拆出对应可选文档。

- 产品与用户场景：
- 交互与状态：
- 验证重点与高风险：
- 发布、迁移与回滚：

## 待澄清

> 暴露假设（D6）。任何不确定的需求/边界/实现选择，留下显式标记，不要默默假设。
> 解决后从本节移除，并在「决策记录」补一行。review 会把残留项当作阻塞信号。

- [NEEDS CLARIFICATION: <具体问题，例如"敏感词库自建还是接第三方？">]

## 需要细节时

> 指针段（保 D2/D4）。本文件只留合约，实现细节放各模块文件，按需读取，避免一次性把无关内容拉进上下文。
> 没有对应模块时删除该行。

- <领域/模块> 的接口与数据 → modules/<module>.md
- <领域/模块> 的接口与数据 → modules/<module>.md
