# project.md — <Project Name>

> 项目级技术决策 + 共享约束。Feature 级文档引用本文件，不重复。

## 版本信息

| 项目 | 值 |
|------|-----|
| 版本 | v1.0 |
| 日期 | YYYY-MM-DD |
| 团队 | |

---

## 业务目标

> 来自 business-alignment 决策。小项目必填，已有项目可省略。

**问题**：（要解决什么问题？当前怎么解决的？）

**目标用户**：

| 角色 | 描述 | 核心诉求 |
|------|------|---------|
| | | |

**MVP 范围**：
- 必须做：
- 不做：

**成功指标**：

| 指标 | 目标值 | 评估时间 |
|------|--------|---------|
| | | |

---

## 架构模式

**选择**：  
**理由**：  
**拒绝**：

---

## 技术选型

> 每个依赖必须有版本，标注使用方（哪个服务/模块/决策用它），并标注是否由其他依赖内置（避免重复安装）。未被任何模块使用的依赖不要列入——留到真正需要时再加。

| 层 | 选择 | 版本 | 使用方 | 内置于 | 理由 | 被拒方案 |
|----|------|------|--------|--------|------|---------|
| Runtime | | | | | | |
| （按项目类型填写：后端框架 / 前端框架 / 数据库 / ORM / 测试 / CSS / 部署） | | | | | | |

> 「内置于」示例：R3F v9 内置 Zustand v5 → 若已用 R3F，单独安装 Zustand 需确认版本一致，否则标注"由 R3F 内置"不重复安装。

---

## 共享决策

> 所有 feature 共用的技术决策。Feature 级 goal.md 引用这里（PD# 前缀），不重复。
> 决策主题根据项目类型填写——后端项目侧重 ID/分页/认证，前端项目侧重状态管理/样式/动画，全栈项目混合。

| # | 决策 | 选择 | 详情 |
|---|------|------|------|
| PD1 | （决策主题） | | （选择 + 详情） |
| PD2 | （决策主题） | | |
| PD3 | （决策主题） | | |
| PD4 | （决策主题） | | |
| PD5 | （决策主题） | | |
| PD6 | （决策主题） | | |

---

## 共享约束

### 安全

- （多租户隔离 / 输入验证 / 敏感数据加密 / 按项目类型填写）

### 性能

- （按项目类型填写：后端 = 查询索引/pageSize/API 超时；前端 = 帧率/首屏/交互响应）

### 兼容性

- 不删除已有 response 字段（后端 API）/ 不删除已有 props（前端组件）
- 新增字段必须可选
- breaking change 必须版本化

### 工程约束

> 来自 technical-design TD6。detail 和 codegen 消费这些约束。

**模块边界**：
- （public/private 的表达方式、import 规则）

**类型不变量**：
- （哪些业务规则用类型编码，哪些用运行时校验）

**测试策略**：
- （单元/集成/E2E 比例、关键路径）

**lint / format / CI**：
- （工具选型、门禁检查项）

**示例代码**：
- （是否需要、放在哪里）

---

> 以下三节后端项目必填，纯前端项目可省略。

## 服务划分

| 服务 | 职责 | 通信方式 | 数据库 |
|------|------|---------|--------|
| | | | |

---

## 部署架构

| 项目 | 值 |
|------|-----|
| 运行环境 | |
| 容器化 | |
| CI/CD | |
| 环境 | dev / staging / prod |
| 监控 | |

---

## 性能指标

| 指标 | 目标 |
|------|------|
| QPS | |
| P99 延迟 | |
| 可用性 | |

---

## Feature 索引

> 由 detail 阶段自动维护，不手动预填。每次 detail 完成后同步更新。

| Feature | 目录 | 状态 | 说明 |
|---------|------|------|------|
