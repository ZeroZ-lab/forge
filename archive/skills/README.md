# Archived Skills

This directory preserves rejected or retired skill decisions without distributing them.

Archived skills:

- must not appear in Claude or Codex plugin manifests;
- must not be treated as current runtime guidance;
- should retain a short reason for retirement and the replacement, if any;
- return through `experiments/skills/` rather than directly to the stable plugin.

